#include <stdio.h>
 void main()
 {
     int i,n;
     printf("enter the array size=");
     scanf("%d",&n);
   int arr[10];
   printf("enter the array elemnts=");
   for(i=0;i<n; i++)
   {
   scanf("%d ",&arr[i]);
   }
}
